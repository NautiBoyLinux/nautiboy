[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "nautiboy"
version = "0.4.0b1"
description = "Open-source Linux controller for Corsair NAUTILUS RS LCD displays"
readme = "README.md"
requires-python = ">=3.11"
license = "GPL-3.0-or-later"
authors = [{name = "Andrew Tyler"}]
dependencies = [
  "Pillow>=10,<13",
  "PySide6>=6.7,<7",
  "pyudev>=0.24,<1",
  "keyring>=25,<26",
]

[project.urls]
Homepage = "https://github.com/NautiBoyLinux/nautiboy"
Repository = "https://github.com/NautiBoyLinux/nautiboy"
Issues = "https://github.com/NautiBoyLinux/nautiboy/issues"

[project.optional-dependencies]
test = ["pytest>=8,<10"]

[project.gui-scripts]
nautiboy = "nautiboy.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/nautiboy"]

[tool.hatch.build.targets.sdist]
include = [
  "/ARTWORK-LICENSE.txt",
  "/ATTRIBUTION.md",
  "/CHANGELOG.md",
  "/LICENSE",
  "/README.md",
  "/artwork",
  "/docs",
  "/packaging",
  "/pyproject.toml",
  "/src",
  "/tests",
]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
